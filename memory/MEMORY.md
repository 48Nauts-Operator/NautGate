# NautGate — repo-local memory index

Auto-memory entries for sessions working inside this repo. Each line points at a memory file in this directory.

(Empty for now. Entries land here as we learn things the blueprint doesn't already document.)
